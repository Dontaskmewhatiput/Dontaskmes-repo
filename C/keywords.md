Cat
fopen(name, "r") - Opens file to read, places handle
fgetc(fp) --> reads one character from handle
putchar(ch) --> prints character to stdout
fclose(fp) --> closes file

* = pointer
argv[] is the arguments placed in
Example: argv[1] = cat ___, where ___ = [1]